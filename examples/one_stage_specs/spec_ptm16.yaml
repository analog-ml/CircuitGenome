# Feasible one-stage op-amp spec for PTM 16 nm HP bulk.
# Technology: ptm16, circuitgenome/sizer/config/tech_ptm16.yaml
# Supply: 0.7 V, 15 µA bias current. One-stage: no Miller cap, so GBW/PM/SR are not modelled.
# Targets assume the highest-gain load variant (active-mirror / folded-cascode); resistor/simple-CS loads need ~6 dB lower gain.
#
# Usage:
#   circuitgenome size circuits/one_stage_opamp/circuit_0001_flat.ckt \
#     --topology one_stage_opamp \
#     --spec examples/one_stage_specs/spec_ptm16.yaml \
#     --tech ptm16

vdd: 0.7            # V — positive supply
vss: 0.0            # V — ground
ibias: 1.5e-5      # A — input bias current (15 µA)
cl: 2.0e-12        # F — output load capacitance

# DC and frequency performance
gain_min_db: 25           # dB — minimum open-loop DC gain

# Power and swing
power_max_w: 5.0e-4      # W  — maximum total quiescent power
output_swing_max_v: 0.55  # V  — 0.15 V headroom from VDD
output_swing_min_v: 0.15   # V  — 0.15 V headroom from VSS
